// count the names which starts from "A"
var names=["Amit" , "Nrupul" , "Manideep" , "Yogesh" , "Aman" ,  "Venu"];
var count=0;
for (var i=0 ; i<names.length ; i++)
  {
;    if(names[i][0]=="A")
    {
      count++;
    }
  }
console.log(count);
